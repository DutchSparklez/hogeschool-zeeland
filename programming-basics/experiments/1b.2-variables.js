// Definieer de drie variabelen één voor één
const ike = 3.14;
const dev = 3 / 4;
const wan = 0.75;

// 3: Make sure that ike changes to 4 before it gets printed
ike = 4;

// Laat nu de huidige toestand van die drie variabelen zien
document.write("ike=" + ike + "; dev=" + dev + "; wan=" + wan);